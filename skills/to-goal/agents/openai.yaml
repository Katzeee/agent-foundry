interface:
  display_name: "To Goal"
  short_description: "Write a compact goal and start prompt"
  default_prompt: "Use $to-goal to write a compact goal document and give me a prompt to start it."
policy:
  allow_implicit_invocation: false
